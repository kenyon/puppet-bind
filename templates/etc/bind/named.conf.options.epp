# Managed by Puppet

options {
<%-
  $bind::options.each |$key, $value| {
    if $value =~ Array {
-%>
    <%= $key %> {
      <%- $value.each |$item| { -%>
        <%= $item %>;
      <%- } -%>
    };
<%- } else { -%>
    <%= $key %> <%= $value %>;
<%-
    }
  }
-%>
};
